# Makes you numb
- type: entity
  parent: MobStatusEffectDebuff
  id: StatusEffectNumbness
  name: Numbness
  components:
    - type: NumbnessStatusEffect

# Flash right now uses old status effect system, yes, there are 2 running. This entity is for new one.
# So I need to create an entity, a component, a system that listens for events on this component and adds old status effect to target.
# What the fuck?
- type: entity
  parent: MobStatusEffectDebuff
  id: StatusEffectBlinded
  name: Blinded
  components:
    - type: BlindnessStatusEffect

- type: entity
  parent: StatusEffectSlowdown
  id: StatusEffectSlowdownNoMobState
  name: Slowdown
  components:
  - type: StatusEffect
    whitelist:
      components:
      - MovementSpeedModifier
      requireAll: true
    blacklist:
      tags:
      - SlowImmune
  - type: MovementModStatusEffect